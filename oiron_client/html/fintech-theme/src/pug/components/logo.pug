mixin logo(type = '', size='medium')
  |
  |
  span.logo-main(class=type+' '+size)
    |
    img(src=assets + brand.fintech.img alt='logo')/
    | #{type !== 'only' ? brand.fintech.name : ''}
  |
  |
