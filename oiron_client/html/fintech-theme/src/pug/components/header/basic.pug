// #### HEADER ####
header.app-bar.header#header
  .container
    .header-content
      nav.nav-menu.flex-grow-1
        |
        |
        .logo.flex-grow-1.start-mobile
          |
          |
          a(href=link.fintech.home)
            include ../logo.pug
            +logo('landscape', 'large')
          |
          |
        nav.user-menu
          |
          |
          a.btn.secondary.waves-effect(href=basicMenuLink !== undefined ? basicMenuLink : link.fintech.login)
            | #{basicMenuText !== undefined ? basicMenuText : $t.common.login}
          |
          |
          span.spacer.vertical-divider.show-md-up
          include ./top-nav/settings
// #### END HEADER ####